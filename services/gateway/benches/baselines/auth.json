{
  "_comment": "Baseline p95 latency targets for Rust gateway auth endpoints (per HARNESS-ENGINEERING-PLAN.md §A6). CI fails if any p95 regresses >10%.",
  "_captured": "2026-07-07",
  "_toolchain": "1.88.0",
  "_machine": "ubuntu-latest (GitHub Actions, 2-core, 7 GB RAM)",
  "endpoints": {
    "register": {
      "description": "POST /api/v2/auth/register — Argon2 hash + 2 DB txns",
      "p50_ms": 45,
      "p95_ms": 50,
      "p99_ms": 60,
      "target_p95_ms": 50,
      "regression_threshold_pct": 10
    },
    "signin": {
      "description": "POST /api/v2/auth/signin — Argon2 verify + session insert",
      "p50_ms": 20,
      "p95_ms": 25,
      "p99_ms": 35,
      "target_p95_ms": 25,
      "regression_threshold_pct": 10
    },
    "refresh": {
      "description": "POST /api/v2/auth/refresh — SHA-256 lookup + session rotate",
      "p50_ms": 4,
      "p95_ms": 8,
      "p99_ms": 12,
      "target_p95_ms": 10,
      "regression_threshold_pct": 10
    },
    "me": {
      "description": "GET /api/v2/me — JWT verify + 1 DB lookup",
      "p50_ms": 2,
      "p95_ms": 4,
      "p99_ms": 8,
      "target_p95_ms": 5,
      "regression_threshold_pct": 10
    },
    "verify_token": {
      "description": "POST /api/v2/auth/verify — JWT verify only (no DB)",
      "p50_ms": 1,
      "p95_ms": 2,
      "p99_ms": 4,
      "target_p95_ms": 5,
      "regression_threshold_pct": 10
    }
  }
}
