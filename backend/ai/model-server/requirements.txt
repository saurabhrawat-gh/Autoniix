# Runtime — protocol/contract layer (this is the always-installable surface)
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# Cache + queue (production runtime)
redis==5.1.1

# Storage (used by real model implementations)
boto3==1.35.40

# GPU model deps — install on the GPU host only.
# torch>=2.4.0
# torchvision>=0.19.0
# onnxruntime-gpu>=1.19.0
# basicsr==1.4.2          # Real-ESRGAN
# realesrgan==0.3.0
# ffmpeg-python==0.2.0
# numpy==1.26.4
# opencv-python-headless==4.10.0.84
